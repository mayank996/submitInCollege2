<% layout('layouts/boilerplate') %>
<div class="row">
    <h1 class="text-center">Edit Room</h1>
    <div class="col-6 offset-md-3">
        <form action="/rooms/<%= room._id %>?_method=PUT" method="POST" novalidate class="validated-form" enctype="multipart/form-data">
            <div class="mb-3">
                <label class="form-label" for="title">Title</label>
                <input class="form-control" type="text" name="room[title]" id="title" value="<%= room.title %>" required>
                <div class="valid-feedback">Looks good!</div>
            </div>
            <div class="mb-3">
                <label class="form-label" for="location">Location</label>
                <input class="form-control" type="text" name="room[location]" id="location" value="<%= room.location %>" required>
                <div class="valid-feedback">Looks good!</div>
            </div>
            <div class="mb-3">
                <label class="form-label" for="price">Room Rent</label>
                <div class="input-group">
                    <span class="input-group-text" id="price-label">&#x20b9;</span>
                    <input type="text" class="form-control" id="price" placeholder="0.00" name="room[price]" aria-label="price" aria-describedby="price-label" value="<%= room.price %>" required>
                    <div class="valid-feedback">Looks good!</div>
                </div>
            </div>
            <div class="mb-3">
                <label class="form-label" for="description">Description</label>
                <textarea class="form-control" type="text" name="room[description]" id="description" required><%= room.description %></textarea>
                <div class="valid-feedback">Looks good!</div>
            </div>
            <div class="mb-3">
                <label for="image" class="form-label">Add image(s)...</label>
                <input class="form-control" type="file" id="image" name="image" multiple>
            </div>
            <div class="mb-3">
                <% room.images.forEach(function(img, i) { %> 
                    <img src="<%= img.thumbnail %>" class="img-thumbnail" alt="">
                    <div class="form-check-inline">
                        <input type="checkbox" id="image-<%= i %>" name="deleteImages[]" value="<%= img.filename %>">
                    </div>
                    <label for="image-<%= i %>">Delete?</label>
                <% }) %> 
            </div>
            <div class="mb-3"><button class="btn btn-info" type="submit">Update Room</button></div>
        </form>
    </div>
</div>
